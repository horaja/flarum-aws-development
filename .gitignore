# Environment variables / Secrets
# Never commit actual secret files. Use .env.example for templates.
.env
*.pem
*.key
credentials.json
secrets/
*.tfvars # Terraform variable files that might contain secrets

# Terraform state files (these should be managed remotely, e.g., in an S3 backend)
terraform.tfstate
terraform.tfstate.backup
.terraform.lock.hcl

# Terraform local operational files
.terraform/

# AWS SAM / Serverless Framework build artifacts
.aws-sam/
.serverless/

# CloudFormation packaged templates (if different from source)
packaged-template.yaml
packaged.yaml

# OS-specific files
.DS_Store
Thumbs.db
Desktop.ini

# IDE / Editor specific
.idea/
.vscode/
*.swp
*.sublime-workspace
*.sublime-project

# Log files
*.log

# Downloaded binaries or large files not meant for version control
# (Be specific if you add any, e.g., downloaded CLIs or large test data)
# *.zip
# *.tar.gz
